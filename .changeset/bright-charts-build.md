---
"react-use-echarts": patch
---

Upgrade the Vite+ toolchain to 0.2.7 and refresh compatible development dependencies. No API changes — static checks, tests, package validation, and size budgets pass on the updated toolchain.
